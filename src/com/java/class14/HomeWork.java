package com.java.class14;

public class HomeWork {

    /**
     * Homework 1:
     * Write a method that takes a string and returns true if the string starts with “hi” and false otherwise.
     * Homework 2:
     * Write a method that takes a string   returns a new string made of 3 copies of the last 2 chars of the original string.
     * The string length will be at least 2.
     * extraEnd(“Hi”) → “HiHiHi”
     * extraEnd(“Hello”) → “lololo”
     * extraEnd(“ab”) → “ababab”
     * Homework 3:
     * Given a string of any length, return a new string where the last 2 chars, if present, are swapped, so “coding” yields “codign”.
     * lastTwo(“coding”) → “codign”
     * lastTwo(“cat”) → “cta”
     * lastTwo(“ab”) → “ba”
     */
    public static void main(String[] args) {

    }
}
